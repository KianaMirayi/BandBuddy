# BandBuddy 2.0.0 QA 清单

## 自动化门禁

- [x] `python -m compileall -q python/worker python/guitar_separator_hq`
- [x] `python -m unittest discover -s python/worker/tests -p "test_*.py" -v`
- [x] HQ6 Python 单测（固定配置、TTA、无 `without_*`、互补重建）
- [x] `pnpm typecheck`
- [x] `pnpm test`
- [x] `pnpm build`
- [x] Windows x64 安装版和便携版启动检查
- [ ] macOS Apple Silicon DMG / ZIP 启动检查（已配置 `macos-15` arm64 CI；需在 macOS runner 执行）
- [x] UI 渲染产物与快照不出现模型名、checkpoint 或权重分支

## ModelScope 与安装环境

- [x] 匿名访问 `Zzzzzzorz/BandBuddy-Models` 的 `v2.0.0` resolve URL
- [x] 四个文件大小和 SHA-256 与代码清单一致，响应是权重实体而不是 Git LFS pointer
- [x] 全新模型缓存只访问固定 ModelScope 仓库
- [x] HTTP Range 断点续传；服务端忽略 Range 时安全从头覆盖 `.part`
- [x] 损坏成品、损坏 `.part`、哈希错误、网络中断、分支不存在均不写完整 marker
- [x] Windows CUDA 和等质量 CPU 回退
- [ ] macOS MPS 回退（实现与 CI 已覆盖，需在 Apple Silicon runner 执行真实设备检查）

## 指定歌曲整曲验收

输入：`C:\CloudMusic\新裤子 - 没有理想的人不伤心.mp3`

- [x] 高音质关闭：九个 44.1 kHz、stereo、320 kbps MP3
- [x] 高音质开启：九个 44.1 kHz、stereo、24-bit FLAC
- [x] 每组九轨时长一致，整曲同步漂移小于 30 ms，无 NaN/Inf
- [x] 编码前 Acoustic / Lead / Rhythm 的 peak、RMS 与 HQ6 基线核对
- [x] `lead + rhythm - electric` float32 peak 误差不超过 `1e-6`
- [x] MP3 与 FLAC 歌曲可同时存在，播放、波形、重分轨、录音和导出不依赖固定扩展名

## 产品行为

- [x] 默认六轨模式；切换后显示八条可见轨且无双重 Guitar
- [x] 模式切换不改写九轨各自 Mute、Solo、gain、输出通道或轨道顺序
- [x] 隐藏轨不进入 Solo、硬件输出、键盘导航、排练/录音伴奏或默认混音导出
- [x] 模式切换 35 ms 交叉淡化，无爆音且不重新缓冲
- [x] 独立分轨导出默认当前模式；“包含隐藏吉他备选轨”按预期工作
- [x] 分别静音 Acoustic、Lead、Rhythm 后可导出对应去除版混音
- [x] 设置修改只影响随后创建的任务；运行中、排队中和已完成任务保持创建时格式
- [x] 旧六轨歌曲提示重分，成功后自动开启；失败/取消后保持原六轨和旧活动版本
- [x] 没有原始音频的历史歌曲可播放/导出，但不能打开吉他分轨
- [x] 新 UI 不含“导入已有分轨”，普通进度和通知只显示“正在分轨”

## 2026-09-03 本机验收记录

- 环境：Windows x64、Python 3.12.10、Torch/Torchaudio 2.11.0、Demucs 4.1.0、RTX 3060；强制隐藏 CUDA 后的真实短推理确认 `device=cpu`。
- 自动化：Vitest `36` 个文件、`157/157` 通过；模型下载器 `9/9` 通过；HQ6 `4/4` 通过；TypeScript、生产构建、Python compileall 与原生音频宿主 self-test 通过。
- 全新产品缓存：`.codex-tmp/model-downloader-fresh-v2-20260903`；四个权重从固定匿名 resolve URL 下载并逐项验证后才生成 `.bundle-complete.json`。
- 指定歌曲原始九轨：`.codex-tmp/worker-nine-full-v2-20260903`；九轨都是 15,040,452 帧 float32，且不含 `without_*`。
- 最终编码：`.codex-tmp/full-nine-flac-v2-20260903-headroom` 与 `.codex-tmp/full-nine-mp3-v2-20260903-headroom`；两组九轨均为 15,040,437 帧（341.052993 秒），组内及组间漂移 `0 ms`，最大解码峰值分别为 `0.998999953` 与 `0.996360004`。
- Acoustic / Lead / Rhythm 与既有 HQ6 基线逐样本差值均为 `0`；重建电吉他的 float32 SHA-256 为 `3c34deb24b272c3b55e1fcf62e3e5fbe77996303434959780d1a469f2ca3aa54`，与基线一致；推理内 `lead + rhythm - electric` peak 为 `2.9802322387695312e-08`。
- UI 实测覆盖设置开关两种文案、六轨/八轨切换和隐藏备选轨导出；生产 renderer 扫描结果为 `NO_PROHIBITED_RENDERER_STRINGS`。
- Windows 产物：`BandBuddy-2.0.0-x64.exe`（SHA-256 `81eaba3cbf83e183441c471dfe25aab4c3e7ccf6fa112f9f1793ca6a9656fe6a`）与 `BandBuddy-2.0.0-x64-portable.exe`（SHA-256 `5edaa18b10f6f581a49021911b5ac8eaddb6f5059671eabbe94deacecccf00a6`）。安装器、安装版、解包版和便携版均返回成功；安装版文件版本为 `2.0.0`。
