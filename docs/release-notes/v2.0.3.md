# BandBuddy 2.0.3

- Remember window size and maximized state; keep waveform timing aligned when resizing.
- Double-click track levels to edit dB values and show explicit and solo-induced mute states.
- Use the themed output-channel selector without triggering playback shortcuts while it has focus.
- Make the M keyboard shortcut restore 0 dB when unmuting a track at the -60 dB silence floor.
- Include expanded audio/video import and FFmpeg preprocessing, signed uv integrity handling, and audio-host Unicode fixes.
- Verify Developer ID signatures for all bundled macOS executables during packaging; distribute notarized Apple Silicon DMG and ZIP builds.

Validation: 251 JavaScript/TypeScript tests and both TypeScript checks passed on the integrated PR before packaging. Artifact verification is recorded separately in the macOS notarization QA report.
