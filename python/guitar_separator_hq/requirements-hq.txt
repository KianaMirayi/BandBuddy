# Public dependencies used by the pinned HQ6 Windows/Python 3.12 implementation.
# The direct official wheel URL prevents installers from silently choosing the
# CPU-only PyPI build with the same public version number.
torch @ https://download.pytorch.org/whl/cu130/torch-2.11.0%2Bcu130-cp312-cp312-win_amd64.whl
numpy==2.5.2
scipy==1.17.0
soundfile==0.14.0
librosa==1.0.0
PyYAML==6.0.3
einops==0.8.1
beartype==0.18.5
rotary-embedding-torch==0.3.5
packaging==26.2
